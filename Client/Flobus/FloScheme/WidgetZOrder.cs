namespace GazRouter.Flobus.FloScheme
{
    public enum WidgetZOrder
    {
        Pipeline = 100,
        PipelineConnectionHint = 200,
        PipelineMarker = 300,
        Valve = 500,
        CompressorShop = 500,
        DistributingStation = 500,
        MeasuringLine = 500,
        ReducingStation = 500,
        Measurings = 800,
        PipelinePoint = 1000,
        Max = 20000
    }
}
